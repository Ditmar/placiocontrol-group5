import React from 'react';
import { MenuGlobal} from '../MenuGlobal';
export default {
    title: 'ui/components/menuglobal',
    component: MenuGlobal,
}

const Template = args => <MenuGlobal {...args}/>
export const DefaultMenuGlobal = Template.bind({});